rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules — tighten before production.
    match /leads/{leadId} {
      allow read, write: if true;
    }
    match /feedbacks/{feedbackId} {
      allow read, write: if true;
    }
  }
}
